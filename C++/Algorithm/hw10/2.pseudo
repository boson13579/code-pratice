
for _ in range(0, |V|):
    for each edge uv ∈ E:
        if (d[v] > d[u] + w(u, v)):
            d[v] = d[u] + w(u, v)
            π[v] = u
        if(d[v] < 0) break;
    now = v;
    do{
        ans.push_back(now)
        now = π[now]
    }while(now != v)
    ans.push_back(now)
    return ans  