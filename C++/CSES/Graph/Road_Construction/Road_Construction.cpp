#include <bits/stdc++.h>
using namespace std;

vector<int> path[100005];




int main() {
    ios::sync_with_stdio(0), cin.tie(0);

    int n, m;
    cin >> n >> m;
    for (int i = 0, a, b; i < m; i++) {
        cin >> a >> b;

    }


    return 0;
}
