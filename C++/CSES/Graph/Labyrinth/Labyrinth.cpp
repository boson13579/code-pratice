#include <bits/stdc++.h>
using namespace std;

char maze[1005][1005];

int main() {
    ios::sync_with_stdio(0), cin.tie(0);

    pair<int, int> start, destination;

    int n, m;
    cin >> n >> m;

    for (int i = 0; i < n; i++) {
        for (int t = 0; t < m; t++) {
			
			

        }
    }
    return 0;
}
